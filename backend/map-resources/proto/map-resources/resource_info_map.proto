package resource_info.proto;

import "aslam/common/id.proto";

message ResourceInfo {
  required int32 folder_idx = 1;
}

message ResourceInfoMap {
  repeated uint32 resource_type = 1;
  repeated aslam.proto.Id resource_id = 2;
  repeated ResourceInfo resource_info = 3;
}
